# En développement, laisser VITE_API_URL vide force l'utilisation
# d'URLs relatives (/api/...) afin que le proxy Vite prenne le relais.
# Ne PAS mettre http://localhost:8000 ici — le proxy gère la redirection.
VITE_API_URL=
